function check(num){
    console.log(num < 0 ? `this is less than 0` : num === 0 ? `this is equal's to 0` : `this is greater than 0`);  
}
check(10)

// syntax

/**
 * condtion ? trueValue : falseValue 
 */