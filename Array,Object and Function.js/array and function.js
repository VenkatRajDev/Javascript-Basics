//object are array with function paramerter
function call(a,b,c,d){// declare the parameters 
let apple = []// declare an empty array 
   apple.push(a)//
   apple.push(b)//push the value into the array using push method..
   apple.push(c)//
   apple.push(d)//
   for(let i=0;i<apple.length;i++){//
    console.log(apple[i])//print the array using for loop...
   }
}
call("ooty apple","more than one apple","not an apple","most wanted apple")//now call the function with arguments
